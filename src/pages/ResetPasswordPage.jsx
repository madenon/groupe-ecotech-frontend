import ResetForm from "../auth/ResetForm";

const ResetPasswordPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-black via-green-900 to-emerald-900 flex items-center justify-center px-4 sm:px-10">
      <ResetForm />
    </div>
  );
};

export default ResetPasswordPage;
